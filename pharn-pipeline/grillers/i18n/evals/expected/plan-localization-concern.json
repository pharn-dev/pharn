{
  "skill_kind": "llm",
  "assertions": {
    "structural": [
      { "kind": "finding_count", "op": "==", "value": 1 },
      { "kind": "field_equals", "field": "type", "value": "FINDING" },
      { "kind": "field_equals", "field": "rule_id", "value": "P7" },
      { "kind": "field_equals", "field": "severity", "value": "important" },
      { "kind": "file_resolves", "value": "pharn-pipeline/grillers/i18n/evals/cases/plan-localization-concern.md:13" }
    ],
    "semantic": [
      {
        "judge": "the scanner reports found:false (no lexical hardcoded-string pattern — the offending strings are a hardcoded 'en-US' locale and a concatenated user-facing summary, neither of which the scanner's high-signal patterns catch), so this is a Layer-2 ADVISORY judgment: exactly one finding (rule_id P7) surfaces the locale-hardcoded formatting + concatenated-user-text localization gap; it is surfaced for the human, never gated."
      }
    ]
  }
}
