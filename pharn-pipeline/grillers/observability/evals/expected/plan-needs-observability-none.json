{
  "skill_kind": "llm",
  "assertions": {
    "structural": [
      { "kind": "finding_count", "op": "==", "value": 1 },
      { "kind": "field_equals", "field": "type", "value": "FINDING" },
      { "kind": "field_equals", "field": "rule_id", "value": "P6" },
      { "kind": "field_equals", "field": "severity", "value": "important" },
      { "kind": "file_resolves", "value": "pharn-pipeline/grillers/observability/evals/cases/plan-needs-observability-none.md:6" }
    ],
    "semantic": [
      {
        "judge": "the scanner finds no observability vocabulary (mentions:false); the griller's ADVISORY layer surfaces that an operationally-significant background job (retrying external webhook deliveries, whose failures matter in prod) is planned with no way to observe it, citing P6 (verify before assert). This is model judgment, explicitly not a deterministic floor claim and never gating; the finding cites the plan's title line as a whole-document absence."
      }
    ]
  }
}
