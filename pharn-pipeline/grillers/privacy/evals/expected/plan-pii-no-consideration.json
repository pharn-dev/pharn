{
  "skill_kind": "llm",
  "assertions": {
    "structural": [
      { "kind": "finding_count", "op": "==", "value": 1 },
      { "kind": "field_equals", "field": "type", "value": "FINDING" },
      { "kind": "field_equals", "field": "rule_id", "value": "P2" },
      { "kind": "field_equals", "field": "severity", "value": "important" },
      { "kind": "file_resolves", "value": "pharn-pipeline/grillers/privacy/evals/cases/plan-pii-no-consideration.md:13" }
    ],
    "semantic": [
      {
        "judge": "the scanner finds no PII literal/field (no FLOOR finding); the griller's ADVISORY layer surfaces that precise location data is collected on every request and retained indefinitely with no minimization/consent/retention consideration — model judgment, explicitly not a deterministic floor claim, and never gating."
      }
    ]
  }
}
